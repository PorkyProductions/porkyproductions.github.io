{
    "configurations": [       
        {
            "command": "npx serve",
            "name": "Run npm start",
            "request": "launch",
            "type": "node-terminal"
        }

    ]
}